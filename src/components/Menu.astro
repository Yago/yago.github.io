<nav class="flex flex-col p-8 gap-6 md:p-14">
  {
    [
      ['/', 'Homepage'],
      ['/projects', 'Projects'],
      ['/blog', 'Blog'],
    ].map(([href, label]) => (
      <a
        href={href}
        class="text-2xl font-light transition-colors duration-200 hover:text-green dark:hover:text-green-dark"
      >
        {label}
      </a>
    ))
  }
</nav>
